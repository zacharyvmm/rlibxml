[package]
name = "rlibxml"
version = "0.1.1"
edition = "2024"

[features]
vendored = []

[build-dependencies]
bindgen = "0.72"
pkg-config = "0.3"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "selectors"
harness = false
